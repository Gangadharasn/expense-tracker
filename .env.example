# Storage: memory | json-file | sqlite | mongodb
# If MONGODB_URI is set, MongoDB is used automatically (recommended for Vercel)
STORAGE_TYPE=MongoDB

# MongoDB Atlas connection string (get from Atlas → Connect → Drivers)
MONGODB_URI=mongodb+srv://gangadharasn98_db_user:o5Qc8IBJCNGSxWHW@cluster0.dcmnyqq.mongodb.net/?appName=Cluster0

# Data directory (for json-file and sqlite — local only)
DATA_DIR=./data

# Server port
PORT=3000

# Login access codes (6 digits each)
GANGADHAR_PIN=251998
KRUTHIKA_PIN=311998

# Session signing secret (change in production)
AUTH_SECRET=your-random-secret-here
